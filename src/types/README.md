# Types - TypeScript Definitions

**index.ts**: Re-exporta tipos Prisma (User, Campaign, Lead, etc.), define tipos de API (requests/responses), tipos de frontend (WizardState), e re-exporta tipos especializados de outros módulos (evita duplicação, mantém single source of truth no Prisma schema).
