# Prisma Database Schema

**schema.prisma**: Define o schema do banco PostgreSQL (8 models: User, UserSettings, Campaign, Lead, Email, WhatsAppMessage, ChannelSendLog). Contém configurações de cadências (email, WhatsApp, híbrido), templates, prompts IA, tracking de envio e enums de status.
